package com.agelousis.cluedonotepad.dialog.models

import androidx.annotation.DrawableRes

data class CharacterRowModel(@DrawableRes val characterIcon: Int, val characterName: String, val characterColor: Int)