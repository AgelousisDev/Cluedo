package com.agelousis.cluedonotepad.splash.enumerations

enum class Language(val locale: String) {
    ENGLISH(locale = "en"),
    GREEK(locale = "el"),
    FRENCH(locale = "fr"),
    GERMAN(locale = "de"),
    ITALIAN(locale = "it")
}